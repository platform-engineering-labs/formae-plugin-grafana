amends "pkl:Project"

dependencies {
  ["grafana"] = import("../../schema/pkl/PklProject")

  // The compose plugin schema is loaded from the installed plugin.
  // Run `make install` in the formae-plugin-compose repo first, then
  // update the path below to match your home directory.
  ["compose"] = import("/home/jeroen/.pel/formae/plugins/compose/v0.1.0/schema/pkl/PklProject")

  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.83.0"
  }
}
