amends "pkl:Project"

dependencies {
  ["grafana"] = import("../../schema/pkl/PklProject")

  // Resolved during `make install` to the installed compose plugin schema.
  // If this path is wrong, run `make install` in the formae-plugin-grafana repo.
  ["compose"] = import("FORMAE_PLUGINS_DIR/compose/v0.1.0/schema/pkl/PklProject")

  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.83.0"
  }
}
